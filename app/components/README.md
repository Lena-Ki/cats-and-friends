# Page sections

Each visual section of the page lives in its own folder. Keep the component,
its CSS module, and any section-only assets together. Shared page copy remains
in `app/EventPage.tsx` until the next content pass moves it into a dedicated
content file.
