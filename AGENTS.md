# Project conventions

- Place every React component in its own folder under `app/components`.
- Name the folder after the component and keep its component file and related CSS module together inside that folder.
- Do not place component-specific styles in `app/globals.css`; reserve that file for shared tokens, resets, and truly reusable global styles.
